{
  "name": "calculatrice-coffre",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "echo ready",
    "cap:add": "npx cap add android",
    "cap:sync": "npx cap sync android"
  },
  "dependencies": {
    "@capacitor/core": "^7.0.0",
    "@capacitor/android": "^7.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^7.0.0"
  }
}